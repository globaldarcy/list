---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Photo">
    <main>
        <div class="grid">
            <div class="grid-item">
                <img src="img/photos/1.jpg" />
            </div>
            <div class="grid-item">
                <img src="img/photos/2.jpg" />
            </div>
            <div class="grid-item">
                <img src="img/photos/3.jpg" />
            </div>
            <div class="grid-item">
                <img src="img/photos/4.jpg" />
            </div>
            <div class="grid-item">
                <img src="img/photos/5.jpg" />
            </div>
            <div class="grid-item">
                <img src="img/photos/6.jpg" />
            </div>
            <div class="grid-item">
                <img src="img/photos/7.jpg" />
            </div>
            <div class="grid-item">
                <img src="img/photos/8.jpg" />
            </div>
            <div class="grid-item">
                <img src="img/photos/9.jpg" />
            </div>
            <div class="grid-item">
                <img src="img/photos/10.jpg" />
            </div>
            <div class="grid-item">
                <img src="img/photos/11.jpg" />
            </div>
            <div class="grid-item">
                <img src="img/photos/12.jpg" />
            </div>
        </div>
    </main>
</Layout>

<style lang="scss">
    @function vw($px, $design-width: 1920) {
        @return calc($px / $design-width) * 100vw;
    }
    // 文字溢出隐藏显示...
    @mixin text-ellipsis($line: 1) {
        @if $line==1 {
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        } @else {
            overflow: hidden;
            text-overflow: ellipsis;
            word-break: break-all;
            display: -webkit-box;
            -webkit-line-clamp: $line;
            -webkit-box-orient: vertical;
        }
    }

    @mixin disabled($grey: false) {
        pointer-events: none;
        user-select: none;
        @if ($grey == true) {
            filter: grayscale(1);
        }
    }

    @mixin sr-only {
        position: absolute;
        width: 1px;
        height: 1px;
        margin: -1px;
        padding: 0;
        overflow: hidden;
        clip: rect(0, 0, 0, 0);
        border: 0;
    }

    @mixin border-radius($param: vw(10)) {
        overflow: hidden;
        border-radius: $param;
        transform: rotate(0deg);
    }
</style>

<script>
    // 方法一：使用 window.screen.orientation 2023年以后浏览器才全部兼容
    // window.addEventListener('resize', () => {
    //     if ([0, 180].includes(window.screen.orientation.angle)) {
    //         console.log("竖屏", window.screen.orientation);
    //     }
    //     if ([90, -90].includes(window.screen.orientation.angle)) {
    //         console.log("横屏", window.screen.orientation);
    //     }
    // })
    // 方法二：使用 window.matchMedia
    function checkOrientation() {
        if (window.matchMedia("(orientation: portrait)").matches) {
            console.log("竖屏");
        } else if (window.matchMedia("(orientation: landscape)").matches) {
            console.log("横屏");
        }
    }

    // 监听屏幕方向变化事件
    // window.matchMedia("(orientation: portrait)").addEventListener("change", checkOrientation);
    // window.matchMedia("(orientation: landscape)").addEventListener("change", checkOrientation);

    // 初始调用
    // checkOrientation();

    window.matchMedia("(orientation: portrait)").addEventListener("change", function () {
        console.log(this);
        if (this.matches) {
            console.log("竖屏");
        }
    });
</script>
